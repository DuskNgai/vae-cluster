# VAE-Clustering

## Setup

First, download the codebase and the submodules:

```bash
git clone https://github.com/DuskNgai/vae-cluster.git vae-cluster --recursive && cd vae-cluster
```

Second, install the dependencies by manually installing them:

```bash
bash install.sh
```
